---
import { cn } from '@/utils/cn';

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<aside
	class={cn('flex items-center justify-center bg-primary py-2', className)}
>
	<slot />
</aside>
